/** PLATFORM_ID value the prerender worker runs under; specs use it to assert server-safe behaviour. */
export const SERVER_PLATFORM_ID = 'server';

/** Default PLATFORM_ID of jsdom-driven specs. */
export const BROWSER_PLATFORM_ID = 'browser';
