/** Route param carrying the normalized athlete key (`/athletes/:key`). */
export const KEY_ROUTE_PARAM = 'key';

/** Select value of the "everything" option in the year and distance filters. */
export const ALL_OPTION_VALUE = '';
