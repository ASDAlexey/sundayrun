/** Thumbnails the strip renders before it collapses the rest into a «+N» counter on the last tile. */
export const PHOTO_STRIP_VISIBLE_COUNT = 3;

/** Keys the open viewer answers to; anything else falls through to the page. */
export const PHOTO_VIEWER_CLOSE_KEY = 'Escape';

export const PHOTO_VIEWER_PREVIOUS_KEY = 'ArrowLeft';

export const PHOTO_VIEWER_NEXT_KEY = 'ArrowRight';
