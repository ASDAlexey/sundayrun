/** The year `<select>` value that disables filtering. */
export const ALL_YEARS_VALUE = 'all';

/** TransferState key of the prerendered race list (the `ng-state` script). */
export const RACES_TRANSFER_KEY = 'races.list';

/** Races in a card's dynamics chart: the race itself plus up to 7 preceding weeks. */
export const TREND_WINDOW_SIZE = 8;
