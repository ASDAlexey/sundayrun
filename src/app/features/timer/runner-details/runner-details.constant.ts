/** The first tap of a runner is his 2.3 km lap, the second his 5 km finish. */
export const TIMER_CARD_LAP_INDEX = 0;
export const TIMER_CARD_FINISH_INDEX = 1;

/** Nothing is written down yet — a dash rather than an empty cell, so the row keeps its height. */
export const TIMER_CARD_NO_TIME = '—';

/** «9:26 · 23:26» — the lap and the finish of one runner on a single line. */
export const TIMER_CARD_TIME_SEPARATOR = ' · ';
