/** One row of «поменять местами с…»: another runner of the same measurement. */
export interface TimerCardRunner {
  fullName: string;
  id: string;
  /** What is already written down for him, so the swap is made with open eyes. */
  timesText: string;
}
