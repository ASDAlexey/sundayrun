/** A row without a gendered place — a 2.3 km runner, a DNF, or a finisher whose gender is unknown. */
export const TIMER_FINISH_NO_PLACE_TEXT = '';

/** The winner of his own group is nobody's chaser — his gap column stays empty. */
export const TIMER_FINISH_NO_GAP_TEXT = '';

/** Behind-the-leader times are signed, so «+2:54» reads as a gap and not as a finish time. */
export const TIMER_FINISH_GAP_PREFIX = '+';

/** Nothing waits for a name — the «времена без имени» block stays away. */
export const TIMER_FINISH_NOTHING_UNASSIGNED = 0;
