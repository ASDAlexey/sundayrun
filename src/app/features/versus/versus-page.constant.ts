/** Route param carrying the first athlete's key (`/vs/:left/:right`). */
export const LEFT_ROUTE_PARAM = 'left';

/** Route param carrying the second athlete's key. */
export const RIGHT_ROUTE_PARAM = 'right';

/** At most this many name matches drop out of the search box. */
export const VERSUS_SUGGESTION_LIMIT = 8;

/** Shown in the gap column when a meeting is a draw. */
export const DRAW_GAP_TEXT = '=';
