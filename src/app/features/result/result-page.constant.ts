export const EVENT_NUMBER_PREFIX = '№ ';

export const SUMMARY_SEPARATOR = ' — ';

export const PDF_MIME_TYPE = 'application/pdf';

export const EMPTY_TEXT = '';

/** The waiting hint's elapsed counter ticks once a second. */
export const PUBLISH_TICK_INTERVAL_MS = 1000;

/** The wait bar tops out short of full — only the freshness probe may declare the deploy done. */
export const DEPLOY_PROGRESS_CAP_PERCENT = 97;

export const PERCENT_FULL = 100;
