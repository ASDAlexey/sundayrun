/** Accepted file extension, compared against the lowercased file name. */
export const XLSX_EXTENSION = '.xlsx';

export const ENTER_KEY = 'Enter';

export const SPACE_KEY = ' ';

/** Keys that activate the drop zone as a button. */
export const ZONE_ACTIVATION_KEYS: readonly string[] = [ENTER_KEY, SPACE_KEY];

export const PREVIEW_ROUTE_COMMANDS: string[] = ['/preview'];

/** Clearing the input lets the same file be selected again. */
export const EMPTY_INPUT_VALUE = '';
