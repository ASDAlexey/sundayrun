export const DNF_TEXT = 'DNF';

/** Shown when the timer recorded no laps at all. */
export const NO_DISTANCE_TEXT = '—';

/** Two recorded laps mean the full 5 km distance. */
export const FULL_DISTANCE_LAPS = 2;

/** A single recorded lap means the short 2.3 km distance. */
export const SHORT_DISTANCE_LAPS = 1;
