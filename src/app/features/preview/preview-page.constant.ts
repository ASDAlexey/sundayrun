/** Router commands for the protocol generation page. */
export const RESULT_ROUTE_COMMANDS: string[] = ['/result'];

/** Size of the history-loading spinner, matching the status text line height. */
export const HISTORY_SPINNER_DIAMETER = 24;
