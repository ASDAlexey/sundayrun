/** Name parts between spaces and hyphens; each part is re-cased independently ('анна-мария' → 'Анна-Мария'). */
export const NAME_PART_PATTERN = /[^\s-]+/g;

export const WHITESPACE_RUN_PATTERN = /\s+/g;

export const SINGLE_SPACE = ' ';
