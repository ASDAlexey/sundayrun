/** 2 recorded laps mean the participant finished the full 5 km. */
export const FIVE_KM_LAP_COUNT = 2;

/** 1 recorded lap means the participant ran 2.3 km only. */
export const TWO_THREE_KM_LAP_COUNT = 1;

/** Position of lap 1 (the 2.3 km split) in `lapsMs`. */
export const FIRST_LAP_INDEX = 0;

/** Protocol rows are numbered starting from 1. */
export const FIRST_ROW_INDEX = 1;

/** Display value for an absent time. */
export const EMPTY_TIME = '';
