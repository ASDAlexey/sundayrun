/** How many latest 5 km finishes make one rolling «Форма» window. */
export const FORM_WINDOW_SIZE = 5;

/** The form percent at the athlete's best-ever rolling median — the peak. */
export const PEAK_PERCENT = 100;
