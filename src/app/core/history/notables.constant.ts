/** Career ranks worth a note: the 2nd and 3rd all-time results (the 1st is the stored «ЛР» note). */
export const NOTABLE_TOP_RANK_LIMIT = 3;

/** A career rank means little in a short history: fewer runs than this and the rank is skipped. */
export const NOTABLE_MIN_CAREER_RUNS = 5;

/** The trailing window of the «лучший результат за 6 месяцев» notable, mirrored in its i18n text. */
export const NOTABLE_WINDOW_MONTHS = 6;

/** The window note needs company: at least this many older runs inside the window. */
export const NOTABLE_MIN_WINDOW_RUNS = 3;

/** 'YYYY-MM-DD' — the date part of an ISO timestamp. */
export const ISO_DATE_LENGTH = 10;
