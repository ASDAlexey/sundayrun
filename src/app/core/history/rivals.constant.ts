/** Finishing within ten seconds of each other reads as «финишировали рядом». */
export const CLOSE_FINISH_GAP_MS = 10_000;

/** A single close finish is a coincidence; a rivalry starts at two. */
export const RIVAL_MIN_CLOSE_COUNT = 2;

/** The «Соперники» card shows at most this many rivals. */
export const RIVALS_LIMIT = 4;
