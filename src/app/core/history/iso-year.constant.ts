/** In 'YYYY-MM-DD' the year is the first 4 characters. */
export const ISO_YEAR_LENGTH = 4;

/** Glued to a year, it names its first day — the lower bound of a «за этот год» window. */
export const ISO_YEAR_START_SUFFIX = '-01-01';
