/** The finish floor a season median needs on both sides of the comparison — fewer is one lucky race. */
export const PROGRESS_MIN_SEASON_FINISHES = 3;

/** The «Прогресс года» leaderboard length on the year review page. */
export const YEAR_PROGRESS_LIMIT = 5;
