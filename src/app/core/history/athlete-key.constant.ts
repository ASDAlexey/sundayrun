/** Any run of whitespace inside a name (collapsed to a single space). */
export const WHITESPACE_RUN_PATTERN = /\s+/g;

/** Cyrillic 'ё' (input is lowercased first, so uppercase 'Ё' is covered too). */
export const YO_LETTER_PATTERN = /ё/g;

export const SINGLE_SPACE = ' ';

export const YE_LETTER = 'е';
