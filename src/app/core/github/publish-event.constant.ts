/** Repository JSON files are pretty-printed for readable diffs. */
export const JSON_INDENT = 2;

/** Joins the first and last date of a batch publication's commit slug. */
export const BATCH_SLUG_RANGE_SEPARATOR = '..';

/** Wrap the batch size into the commit slug: `2026-01-04..2026-02-22 (8)`. */
export const BATCH_SLUG_SUFFIX_OPEN = ' (';
export const BATCH_SLUG_SUFFIX_CLOSE = ')';
