/** What the browser asks before a navigation, a closed tab or a «←» takes a running race off screen. */
export const TIMER_LEAVE_CONFIRM = $localize`:@@timer.leaveConfirm:Забег ещё идёт. Точно уйти с экрана?`;

/** The event that fires when the tab itself is about to go; preventing it raises the browser's own dialog. */
export const BEFORE_UNLOAD_EVENT = 'beforeunload';
