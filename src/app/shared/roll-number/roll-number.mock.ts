/** A countdown unit at rest. */
export const ROLL_INITIAL = '29';

/** One second later — only the last digit moves. */
export const ROLL_NEXT = '28';

/** The number losing a character, so a slot has to disappear rather than roll. */
export const ROLL_SHORTER = '7';
