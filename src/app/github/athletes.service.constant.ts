/** Prefix of the error thrown when the CDN answers the athletes history read with an unexpected non-OK status. */
export const ATHLETES_HISTORY_LOAD_ERROR_PREFIX = 'Failed to load the athletes history: HTTP ';
