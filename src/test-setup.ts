import { setupAutoSpy } from 'vitest-auto-spy/setup';

// Восстанавливает свойства, пропатченные через `mockValueProp` / `mockReadonlyProp`, глобальным
// afterEach. Спеки запускаются с `isolate: false`, то есть в одном окружении подряд, и патч,
// который спека не сняла сама, доживает до следующего файла и падает уже в нём. Две спеки
// (`result-page`, `nav-rail`) как раз ничего не восстанавливали.
// Заодно вторая копия библиотеки в графе зависимостей теперь валит прогон, а не молча
// разводит два реестра моков.
setupAutoSpy();
